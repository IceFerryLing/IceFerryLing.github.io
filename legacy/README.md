# legacy 归档说明

这个目录存放历史框架与实验文件，仅用于备份与参考，不参与当前构建。

- `legacy/jekyll/`：早期 Jekyll 版本文件
- `legacy/next-incompatible-routes/`：Next.js 迁移期遗留说明
- `legacy/archived-root/`：从项目根目录迁移下来的旧框架目录（`app/`、`_posts/`、`_layouts/`、旧 `assets/` 等）

当前线上版本为 **Astro 静态博客**（主目录结构：`src/`、`public/`、`content/`）。
